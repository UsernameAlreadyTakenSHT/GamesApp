package com.manywords.softworks.tafl.ui.lanterna.settings;

/** Stub of the desktop UI's settings: Variants only clamps the selected variant index. */
public final class TerminalSettings {
    public static int variant = 0;

    private TerminalSettings() {}
}
